# This file is maintained for compatibility with older pip versions.
# Dependencies are managed in pyproject.toml.
#
# For production install:
#   pip install -e .
#
# For development install (includes testing, linting, etc.):
#   pip install -e ".[dev]"

# Core dependencies (mirrored from pyproject.toml)
requests>=2.31.0
selenium>=4.15.0
pandas>=2.1.0
webdriver-manager>=4.0.0
typer>=0.9.0
tqdm>=4.66.0
python-dateutil>=2.8.2
pydantic>=2.5.0
